{% set classes = {
    error: 'bg-error text-white',
    notice: 'bg-success text-white',
    info: 'bg-gray-50 border-gray-400 text-gray-800'
} %}

<div class="rounded p-4 mt-4 border {{ classes[type] }}">
    <div class="flex">
        <div class="ml-3">
            <p class="text-sm font-medium">
                {% block message %}
                    {{ message }}
                {% endblock %}
            </p>
        </div>
    </div>
</div>
